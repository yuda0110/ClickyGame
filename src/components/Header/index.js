import React from 'react'
import './style.css'

const Header = props => {
  return (
    <header>
      <h1>Clicky Game!</h1>
      <p>Click on an image to earn points, but don't click on any more than once!</p>
    </header>
  )
}